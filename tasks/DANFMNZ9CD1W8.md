---
title: Close task code action
priority: 60
tags: cli, lsp
---

Currently we only have the single mint code action,

Add a new one, closing a task when on a `<marker>(<id>):`

Need to update the task in place can probably use the vscode document to
diff/send back the change
