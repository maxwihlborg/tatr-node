---
title: "Make bad task show up in list instead of stderr"
priority: 60
closed: true
tags: cli, list
---

Instead of printing to stdout, let's do rows with high prio `999` and set tile
to `!!BROKEN` and make it part of the normal `<id>: prio, tag title` output.
